1.
```c++
#include <iostream>
#include <vector>
using namespace std;

vector<int> solution(vector<vector<int> > v) {
    vector<int> ans(2, 0);
    
	if(v[0][0] == v[1][0])ans[0] = v[2][0]; 
	else if(v[0][0] == v[2][0]){ ans[0] = v[1][0];}
	else if(v[1][0] == v[2][0]){ ans[0] = v[0][0];}
    
    if(v[0][1] == v[1][1]) ans[1] = v[2][1];
    else if(v[0][1] == v[2][1]) ans[1] = v[1][1];
    else if(v[1][1] == v[2][1]) ans[1] = v[0][1];
    
    return ans;
}

int main(void){	
	vector<vector<int> > problem(3, vector<int>(2,0)); // >>를 붙여쓰면 쉬프트 연산자로 인식할 수 있음
    vector<int> ans;
    
    for(int i=0; i < 3; i++){
        for(int j=0; j < 2; j++){

            cin >> problem[i][j];
        }
    }
    for(int i=0; i < 3; i++){
        for(int j=0; j < 2; j++){
            cout << problem[i][j] << "\n";
        }
    }

    ans = solution(problem);
    
    cout << "[" << ans[0] << ", ";
	cout << ans[1] << "]" << endl;
    
}
```
2. 
```c++
#include <iostream>
#include <vector>
using namespace std;

int main(void){	
	int x ,y ;
	cin >> x >> y;
	cin.ignore();

	for(int i=0; i < y; ++i){
		for(int j = 0; j < x; ++j){
			cout << "*";
		}
		cout << "\n";
	}
}
```

